describe.skip('recipe test case in serverside', () => {

    /**
     * The following test cannot be test in Jest,
     * however we can use selinum for testing.
     */
    it('return 404 not found http response if recipe does not existed',
        () => {

        }
    );

    it('does not throw error if json format incorrect', () => {

    });

    it('can return json by provided url path',
        () => {

        }
    );
});
